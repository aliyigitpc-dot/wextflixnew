import { Header } from '@/components/Header'
import { Footer } from '@/components/Footer'

export default function HakkimizdaPage() {
  return (
    <div style={{ minHeight: '100vh', background: 'var(--background)' }}>
      <Header />
      <main style={{ padding: '8rem 2rem 5rem', maxWidth: '48rem', margin: '0 auto' }}>
        <h1 style={{ fontFamily: "'Bebas Neue', Impact, sans-serif", fontSize: 'clamp(3rem, 8vw, 5rem)', marginBottom: '2rem', color: 'var(--foreground)' }}>Hakkımızda</h1>
        <div style={{ display: 'flex', flexDirection: 'column', gap: '1.5rem', color: 'var(--muted-foreground)', lineHeight: 1.75 }}>
          <p style={{ fontSize: '1.0625rem', color: 'var(--foreground)' }}>
            Wexflix, <strong style={{ color: 'var(--primary)' }}>WDB</strong> şirketi tarafından üretilen bir <em>dizi-karikatür</em> platformudur.
          </p>
          <p>
            Sıradan dizi platformlarından farklı olarak Wexflix, çizgi ile gerçeği, karikatür ile dramı aynı sahnede buluşturur. İlk projemiz
            <strong style={{ color: 'var(--foreground)' }}> Sikkolik: The New Era</strong> — Tunahan, Alp, Apo Abi ve Enişte'nin etrafında dönen, parayı kovalayanların kendini kaybettiği bir suç-dram evreni.
          </p>
          <p>
            Wexflix <strong style={{ color: 'var(--primary)' }}>Elyas</strong> tarafından desteklenmektedir.
          </p>
          <div style={{ display: 'grid', gridTemplateColumns: 'repeat(3, 1fr)', gap: '1rem', paddingTop: '1.5rem' }}>
            {[{ label: 'Stüdyo', value: 'WDB' }, { label: 'Destekçi', value: 'Elyas' }, { label: 'İlk Sezon', value: '2026' }].map((s) => (
              <div key={s.label} style={{ borderRadius: '8px', border: '1px solid rgba(46,43,40,0.4)', background: 'rgba(30,27,24,0.4)', padding: '1rem' }}>
                <div style={{ fontSize: '0.75rem', textTransform: 'uppercase', letterSpacing: '0.1em', color: 'var(--muted-foreground)' }}>{s.label}</div>
                <div style={{ marginTop: '4px', fontFamily: "'Bebas Neue', Impact, sans-serif", fontSize: '1.5rem', color: 'var(--foreground)' }}>{s.value}</div>
              </div>
            ))}
          </div>
          <p style={{ fontSize: '0.75rem', paddingTop: '1.5rem', borderTop: '1px solid rgba(46,43,40,0.4)' }}>
            Wexflix kurgusal bir demo projesidir.
          </p>
        </div>
      </main>
      <Footer />
    </div>
  )
}
